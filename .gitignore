# Dependencies
node_modules/

# Next.js
.next/
out/

# Env — NEVER commit secrets. Ignore every .env variant (allow only *.example)
.env
.env.*
*.env
!.env.example
!.env.*.example

# Debug / IDE
.cursor/
*.log
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel

# Debug screenshots dropped in the repo root
Screenshot*.png

# Internal SEO/GEO notes — do NOT upload
SEO-GEO-TODO.md
MARKETING-DRAFTS.md

# Internal audit findings — do NOT upload
WEBSITE-AUDIT-FINDINGS.md

# Claude Code local settings
.claude/

# Gift-card / voucher exports — contain LIVE redeemable URLs (bearer secrets). NEVER commit.
voucher_*.csv
*_val*.csv
supabase/.temp/
